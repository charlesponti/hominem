diff --git a/build/utils/resolveModule.js b/build/utils/resolveModule.js
index 5e50fcb68405f47e1be5aab32d2526d899bed20e..d5b09b18a313ca18e7b02f43ce03a1d1c3fb6b6f 100644
--- a/build/utils/resolveModule.js
+++ b/build/utils/resolveModule.js
@@ -13,6 +13,9 @@ function resolveModule(api, id) {
     }
     let resolved = _cache[id];
     if (resolved !== undefined) {
+        if (id === 'expo-router/package.json' && process.env.__DIAG_ROUTER_RESOLVE__) {
+            console.error('[DIAG resolveModule cached]', { id, possibleProjectRoot, cached: resolved });
+        }
         return resolved;
     }
     try {
@@ -30,6 +33,15 @@ function resolveModule(api, id) {
     }
     finally {
         _cache[id] = resolved;
+        if (id === 'expo-router/package.json' && process.env.__DIAG_ROUTER_RESOLVE__) {
+            console.error('[DIAG resolveModule]', {
+                id,
+                possibleProjectRoot,
+                dirname: __dirname,
+                cwd: process.cwd(),
+                resolved,
+            });
+        }
     }
     return resolved;
 }
