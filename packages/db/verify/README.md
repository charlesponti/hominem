# Verify Scripts

Each file in this directory verifies the contract of one Sqitch change.

Typical checks:

- object exists
- object has expected shape
- policy exists
- function signature matches expectation
- index exists
