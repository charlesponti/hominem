# Served by portless (see /portless.json) at a stable name, independent of
# whatever port the api/career dev servers actually land on.
VITE_PUBLIC_API_URL=https://api.localhost
PUBLIC_APP_URL=https://career.localhost
# Required server-only base URL for session checks. Use the API's Railway-private URL in production.
HOMINEM_INTERNAL_API_URL=https://api.localhost

# AI
OPENROUTER_API_KEY=

# Data
DATABASE_URL=

# Storage
R2_ENDPOINT=http://localhost:9000
R2_PUBLIC_URL=http://localhost:9000
R2_BUCKET_NAME=storage
R2_ACCESS_KEY_ID=minioadmin
R2_SECRET_ACCESS_KEY=minioadmin
